import { Box, styled } from "@mui/material";

export const CustomizedBox = styled(Box)(() => ({
  backgroundColor: "#faebd7",
  boxShadow: "rgba(0, 0,0, 0.05) 0 6 24px, rgba(0,0,0 0.08) 0 0 0 1px",
  borderRadius: "10px",
  padding: "7rem",
  width: "800px",
  marginTop: "50px",
}));
