{
  "name": "pern_to_do_list",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "if-env TYPE=server && (cd server && npm install) && if-env TYPE=client && (cd client && npm install)",
    "server": "node ./server/server.js",
    "client": "cd client && npm start"
  },
  "author": "Long Phan",
  "license": "ISC",
  "dependencies": {
    "if-env": "^1.0.4"
  }
}
